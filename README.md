# mta_gui

MTA_GUI is a gui interface that gives real-time updates about the New York subway (MTA). Allows overview of all trains, 
search functions for trains, routes, and more.

# Requirements

This project was built with Qt Creator using Qt 6.9.3 (Qt Widgets)\
Import the CMakeLists.txt into Qt Creator to open file
